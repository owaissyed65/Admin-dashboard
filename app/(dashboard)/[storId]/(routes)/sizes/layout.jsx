import React from "react";
export const metadata = {
    title: "Sizes",
    description: "Sizes for products",
  };
const BillBoardLayout = ({ children }) => {
  return <div>{children}</div>;
};

export default BillBoardLayout;
