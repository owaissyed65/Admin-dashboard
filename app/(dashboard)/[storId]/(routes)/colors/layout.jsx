import React from "react";
export const metadata = {
    title: "Colors",
    description: "Colors for products",
  };
const BillBoardLayout = ({ children }) => {
  return <div>{children}</div>;
};

export default BillBoardLayout;
