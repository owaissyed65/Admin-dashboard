import React from "react";
export const metadata = {
    title: "Products",
    description: "products of items",
  };
const BillBoardLayout = ({ children }) => {
  return <div>{children}</div>;
};

export default BillBoardLayout;
